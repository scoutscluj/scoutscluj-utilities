{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "scoutscluj-infra": "bin/scoutscluj-infra.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm typecheck",
    "test": "echo \"No infra tests configured yet\"",
    "verify": "pnpm typecheck"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.260.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.1128.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
